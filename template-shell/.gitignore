node_modules
dist
dist-lib
dist-ssr
.__mf__temp
.mf
.vite

# Auto-generated MFE manifests (regenerated by `npm run generate:mfe-manifests`)
public/generated-mfe-manifests.json

# Composed MFE packages copied in from template-mfe for local dev/testing
# (see CONTRIBUTING.md's dev loop) -- empty in a fresh seeded project until
# `frontx add frontx-template-mfe`, regenerated by the documented compose step.
src-app/mfe_packages/*

.DS_Store
*.local
.env
.env.local
.env.*.local
